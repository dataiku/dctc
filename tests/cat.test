#! /bin/sh

. ./utils

exit_status=0

run_cat() {
    run 0 - "cat $*" ../dctc-jar.sh cat "$@" <<.
$(cat $@)
.
    if $fail; then
        exit_status=1
    fi
}


run_cat /etc/bash.bashrc
run_cat /etc/hosts

echo $exit_status
