#! /bin/sh

. ./utils

exit_status=0

run_cmp() {
    run 0 - "cmp $*" dctc cmp "$@" <<.
$(cmp $@)
.
}

run_cmp /etc/bash.bashrc /etc/bash.bashrc
run_cmp /etc/bash.bashrc /etc/bash.bashrc 0 1


echo $exit_status
