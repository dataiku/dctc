#! /bin/sh

. ./utils

exit_status=0

run_cmp() {
    $(cmp $@ > out.exp) || true
    run 0 out.exp "cmp $*" ../dctc-jar.sh cmp "$@"
    if $fail; then
        exit_status=1
    fi
}

run_cmp /etc/bash.bashrc /etc/bash.bashrc
run_cmp /etc/bash.bashrc /etc/bash.bashrc 0 1

echo $exit_status
